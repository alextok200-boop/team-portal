# 团队门户（静态版）

内部权限门户的 **GitHub Pages 静态部署版**。

> 由 Node 后端版（`login-portal` v2.0.0）改造而来，去掉后端，改为**纯前端**实现，
> 以便免费托管在 GitHub Pages。

## ✨ 与后端版的差异

| 能力 | 后端版（login-portal） | 本静态版 |
|---|---|---|
| 登录鉴权 | 服务端 scrypt 哈希 | **前端 SHA-256 哈希比对** |
| 用户/角色管理 | 服务端落盘，全员共享 | **localStorage 本地存储**（admin 后台配置仅本浏览器生效） |
| 日报数据 | 服务端每日 11:05 自动抓取 | **静态 JSON 快照**（本地抓取后推送更新） |
| 会话 | HMAC 签名令牌 | localStorage 令牌（8 小时） |
| 部署 | 需 Node 服务器 | **GitHub Pages 免费静态托管** |

> ⚠️ **静态版是「弱鉴权」**：源码可见、前端密码哈希可被绕过，仅供内部轻量使用。
> 若需要真正的服务端鉴权与共享用户管理，请用后端版（`C:\Users\alext\Web\login-portal`）。

## 🚀 访问地址

部署后访问：`https://alextok200-boop.github.io/team-portal/`

## 🔑 默认账号

| 账号 | 密码 | 角色 |
|---|---|---|
| `admin` | `admin2026` | 管理员（全部权限） |
| `leader` | `team2026` | 团队主管 |
| `member` | `view2026` | 团队成员 |

⚠️ 密码以 SHA-256 哈希存储，登录时前端比对。
账号有**三个来源**（合并去重，优先级从高到低）：

| 来源 | 位置 | 生效范围 |
|---|---|---|
| 本机覆盖层 | 浏览器 localStorage | 仅管理员这台浏览器 |
| **仓库共享名单** | `data/users.json` | **全站 / 所有同事** |
| 源码内置 | `js/config/roles.js` | 全站 / 所有同事 |

> ⚠️ **新增用户后必须「导出并提交」，否则同事登不上。**
> 门户托管在 GitHub Pages（纯静态），浏览器没有服务端可写 ——
> 后台点「新增用户」只落在你这台电脑，属于「待发布」状态。
> 完整流程见下方「2. 登录与用户账号」。

## 📂 目录结构

```
team-portal/
├── index.html              # 首页（加入我们展示 + 视频背景 + 右上角登录按钮）
├── login.html              # 登录页（同样以「加入我们」为主要展示内容 + 右侧吸顶登录栏）
├── css/portal.css          # 门户样式（深色霓虹）
├── js/
│   ├── api.js              # 静态版本地数据层（模拟原后端 API）
│   ├── auth.js             # 认证与权限
│   ├── portal.js           # 顶栏 / 守卫 / 提示
│   ├── board.js            # 业务数据看板的图表与聚合逻辑（ECharts）
│   ├── landing.js          # ★ 加入我们：岗位数据 + 渲染（index / login / careers 三页共用一份）
│   └── config/roles.js     # 默认角色 + 默认用户（密码哈希）
├── pages/                  # 业务页面
│   ├── board.html          # 业务数据看板（登录后通用首页，指标卡可钻取）
│   ├── tables.html         # 数据中心（9 张表浏览 / 搜索 / 钻取）
│   ├── dashboard.html      # 工作台
│   ├── admin.html          # 管理后台（用户/角色/数据/日志）
│   ├── data.html           # 日报数据（钉钉快照）
│   ├── careers.html        # 加入我们（登录后内部版，与 js/landing.js 共用岗位数据）
│   ├── metrics.html        # 数据指标
│   ├── members.html        # 成员管理
│   ├── content.html        # 内容管理
│   ├── settings.html       # 系统设置
│   ├── logs.html           # 登录日志
│   ├── denied.html         # 无权访问
│   └── 404.html
├── data/daily.json              # 日报数据快照（钉钉 AI 表格，Actions 自动更新）
├── data/users.json              # ★ 共享账号名单（新增用户导出后提交到这里，全站生效）
├── assets/showcase.mp4          # 首页背景视频（1080p H.264，约 3.9MB）
├── assets/favicon.svg           # 站点图标
├── vendor/echarts.min.js        # ECharts 5.5.1（已入库，**不外链 CDN**）
├── scripts/fetch-openapi.js     # 钉钉 OpenAPI 抓取脚本（纯 OpenAPI，无需本机）
├── .github/workflows/fetch.yml  # GitHub Actions 定时抓取（每日 11:05）
├── tools/sync-local.sh          # 本地仓库对齐远端（API 推送后必跑）
├── tools/fetch-remote-commit.py # 上述脚本的 API 兜底（github.com 不通时用）
├── tools/add-user.py            # ★ 命令行加/改/删账号（直接写 data/users.json）
├── tools/regress-user-flow.js   # ★ 账号全链路回归（加号→登录→导出→删除 → 26 项断言）
└── .nojekyll                    # 防 Jekyll 处理
```

**导航顺序**（登录后顶栏）：业务数据看板 → 工作台 → 日报数据 → 数据中心 → 数据指标 → 内容管理 → 成员管理 → 加入我们 → 管理后台 → 系统设置 → 登录日志

**角色权限**（`js/config/roles.js` 的 `DEFAULT_ROLES`）：

| 角色 | 可访问页面 |
|---|---|
| `admin` | `['*']` 全部 |
| `team_leader` | board / dashboard / data / tables / metrics / members / careers |
| `member` | board / dashboard / data / tables / content / careers |


## ⚙️ 关键实现说明

### 1. base 前缀（GitHub Pages 子路径部署）

本项目部署在 `/team-portal/` 子路径，所有资源引用采用**相对路径 + 静态 `<base>` 标签**：

- 每个 HTML `<head>` 顶部有 `<base href="/team-portal/">`
- 资源引用（`css/...`、`js/...`、`pages/...`）为相对路径，由 `<base>` 统一解析
- JS 里的跳转通过 `Site.url('/xxx')` 拼接、权限比对通过 `Site.strip()` 剥前缀

> ⚠️ **若改了仓库名，需同步修改每个 HTML 里的 `<base href="/team-portal/">`**
> 为 `/<新仓库名>/`。

### 2. 登录与用户账号（前端 · 三源合并）

`js/api.js` 把原后端 `/api/*` 调用映射到本地实现。用户表由**三层合并**而成，按 `username`（忽略大小写）去重，高优先级整体覆盖低优先级：

```
①  js/config/roles.js  DEFAULT_USERS   源码内置   全站生效   （admin / leader / member）
②  data/users.json     共享名单        随仓库部署 全站生效   ← 新增用户靠它
③  localStorage        本机覆盖层      仅管理员本机
```

- 登录：`crypto.subtle.digest('SHA-256', 密码)` 与哈希比对；新哈希带 `sha256$` 前缀，同时兼容历史无前缀的裸 SHA-256
- 会话：localStorage 存令牌（8 小时过期）
- 角色/日志：localStorage 持久化（角色首次用 `roles.js` 默认值）
- `lastLogin` 单独存在 `portal_lastlogin`，**登录不再回写整张用户表** —— 否则成员浏览器会留下陈旧快照，把管理员后来在仓库里的改动盖回去
- 本机覆盖层只落「与共享名单不等价」的差异记录，等价的陈旧副本会被自动清理

#### ⚠️ 新增用户为什么必须「导出 + 提交」

GitHub Pages 是纯静态托管，**浏览器没有服务端可写**。所以在管理后台点「新增用户」，本质上只是写进了你**这台电脑的 localStorage**：

| 场景 | 结果 |
|---|---|
| 在**建号那台浏览器**里登录 | ✅ 立即可以 |
| 同事在**自己的电脑 / 别的浏览器**登录 | ❌ 报「账号或密码错误」（他的浏览器里没有这条记录） |

要让账号全站生效，二选一：

**A. 管理后台导出（推荐给日常操作）**

```
管理后台 → 用户管理 → 导出用户配置
  → 浏览器下载 users.json
  → 覆盖仓库 data/users.json
  → git add data/users.json && git commit && git push
```

导出的文件只含**与源码/仓库不等价**的账号（内置的 admin/leader/member 不会重复导出），哈希带 `sha256$` 前缀，删除账号会记进 `deleted` 列表以覆盖源码内置账号。

**B. 命令行（适合加号后顺手提交）**

```bash
python tools/add-user.py --user zhangsan --pass konllen2026 --name 张三 --role member
python tools/add-user.py --user zhangsan --pass newpass2026   # 改密
python tools/add-user.py --del zhangsan                       # 删除（写 deleted 墓碑）
python tools/add-user.py --list                               # 看当前名单
```

**管理后台的「来源」列**会标出每个账号来自哪里：`源码` / `仓库` / `待发布`（仅本机）。看到 `待发布` 就说明还没提交。

> ⚠️ **不要用内网 IP（`http://192.168.x.x`）访问门户**：那不是浏览器的「安全上下文」，
> `crypto.subtle` 不可用，无法校验密码。此类访问会**明确报错**而不是静默失败 ——
> 历史版本在这里会降级成 8 位 djb2 散列，结果是在内网 IP 下建的号换成 https 后**永远登不上**，
> 该降级路径已于 v1.4.0 移除。
> 例外：本机用 `http://localhost:8971` / `http://127.0.0.1:8971` 是安全上下文，可以正常登录。

### 3. 日报数据（钉钉自动抓取链路）

`data/daily.json` 是钉钉 AI 表格《电商营销备战-日报追踪表》的静态快照，**全自动更新**：

```
钉钉 AI 表格 → GitHub Actions（每天 11:05）→ scripts/fetch-openapi.js
             → data/daily.json → 自动 commit/push → 网站自动更新
```

- 数据源 baseId：`OG9lyrgJPzYDzl1ESvXRdpEYWzN67Mw4`（共 17 张表，抓取其中 9 张）
- 定时器：`.github/workflows/fetch.yml`，**每日两次** —— UTC 03:05（北京 11:05）与 UTC 10:30（北京 18:30），另支持手动 `workflow_dispatch`
- 每表行数上限：**双层设计** —— `rawCap`（扫描多少原始行，默认 **20000**，分店铺表 16000/24000）+ `keepCap`（保留多少有效行，默认 8000）
- ⚠️ **为什么要双层**：日报追踪表里预建了整年的**空白模板行**（只有日期/星期/促销节点）。单一上限会被空白行挤占，导致真数据被截断 —— 200 行上限时国内表只捞到 24 条有效记录，实际有 799 条
- 默认上限定到 20000 的依据（2026-09-10 实测）：9 张表里最长的两张（投放 / 内容）各有 **7938 行**；上限若低于表长，`truncated` 会恒为真、误报「数据可能不完整」。上限只是天花板，表短则自然扫到表尾、不额外耗时
- 空白模板行默认**丢弃**（`DINGTALK_DROP_EMPTY=0` 可保留），并在 payload 里报告 `rawRows` / `droppedEmptyRows`，丢弃不无声
- 抓取脚本带**指数退避重试**（网络抖动不再整表失败），输出 `truncated` / `emptyTables` / `failedTables` 元数据 —— **截断、空表、失败三种状态不再混淆**
- 「空表」= 整表扫完（未到上限）却零有效行，属模板表尚未录入的正常状态；看板读 `emptyTables` 自动列出，**不在前端硬编码表名**
- 所需 4 个 GitHub Secret：`DINGTALK_APP_KEY` / `DINGTALK_APP_SECRET` / `DINGTALK_OPERATOR_ID` / `DINGTALK_BASE_ID`

> ⚠️ **钉钉 OpenAPI 拿不到「公式 / 查找引用 / 关联引用 / 自动编号」字段**（官方限制）。
> 日报总览的 GMV / 订单 / UV 是公式字段，所以看板核心指标**改从「分店铺日报」聚合**
> （分店铺的 GMV / 订单 / UV 是手工录入，可以拿到）。

#### 六张表的真相（2026-09-10 实测确认，勿再当 bug 排查）

`1.日报总览 / 3.投放日报 / 4.内容达人日报 / 5.流量竞品日报 / 6.供应链客服日报 / 7.团队战力评估`
这 6 张表**全表扫完仍零有效行**，看板故不出图。已用 `row_cap=20000` 的实测运行逐表验证：

| 表 | 表内总行数 | 有效行 | 判定依据 |
|---|---|---|---|
| 1.日报总览 | 162 | 0 | 非结构字段仅 `日目标` / `达成率__旧`，**公式字段、OpenAPI 不返回** |
| 3.投放日报 | 7938 | 0 | 扫到表尾（7938 < 20000 上限）→ **整表空白模板行** |
| 4.内容达人日报 | 7938 | 0 | 同上 |
| 5.流量竞品日报 | 162 | 0 | 同上 |
| 6.供应链客服日报 | 162 | 0 | 同上 |
| 7.团队战力评估 | 24 | 0 | 同上 |

**判据**：扫描行数小于上限 → 是自然扫到表尾，不是被截断；`丢空白 == 扫描` → 每一行都被判空。
两者同时成立即「表还没人录入」，属正常状态。

> 这意味着：**这些表一旦有人开始填数，看板会自动出图，无需改代码**。
> 因为模板行是预建的，用户是「填进已有行」——所以不要加「连续空白 N 行就提前退出」这类优化，
> 会把后填的行一起跳过。

手动补跑：Actions 页面 → `fetch` workflow → `Run workflow`。

### 4. 本地 Git 同步（重要）
本项目的推送常需走 **Git Data API 绕行**（blob → tree → commit → ref），
因为本机 `git push` 走 github.com:443 会被 reset / SSL 中断。**API 推送不会回写本地 git**，
长期下来本地历史会与远端分叉（本地 `git log` 看不到真实版本、`git pull` 冲突、本地无法当回滚点）。

因此：**凡做过 API 推送，或不确定本地是否落后，跑一次**

```bash
bash tools/sync-local.sh            # 对齐；工作区有未提交改动则先备份并停下（不丢东西）
bash tools/sync-local.sh --check    # 只报告差多少，不动文件（落后时退出码 2）
bash tools/sync-local.sh --force    # 确认丢弃未提交改动，强制对齐
```

> 🔒 **防误删闸门**：工作区有未提交改动时，脚本会先整包备份到 `../_backup/`，
> 然后**停下并列出**将被丢弃的文件，必须显式加 `--force` 才继续。
> （这条闸门是踩坑换来的：早期版本直接 reset，把刚改好的文件无声冲掉了。）

脚本走**双通路**，因为本机 `github.com` 时通时断：

| 通路 | 触发条件 | 做法 |
|---|---|---|
| ① `git fetch` | github.com:443 可达 | 直接取回对象（首选） |
| ② `api.github.com` 兜底 | fetch 被 reset / 502 / SSL eof | `tools/fetch-remote-commit.py` 从 API 取远端 SHA，**在本地重建同 SHA 的 commit 对象**，再 reset |

另有两个环境坑已在脚本内处理：
- **代理干扰**：脚本开头 `unset http_proxy/https_proxy/...`（沙箱注入的代理常让 github.com 的 CONNECT 隧道 502）
- **`git update-ref` 静默失效**：受限环境下 git 写 `.git/refs/remotes/**` 会无声失败，导致 `origin/main` 解析不到、`git status` 永远显示 up-to-date —— 脚本用幂等直写兜底

> ⚠️ `git reset --hard` 会删掉「只在旧提交里存在」的文件（实测踩过：回退时把整个 `tools/` 目录删了）。
> 该脚本只用于**向前对齐**，不要拿它当回退工具。
>
> 正常网络下 `git push` / `git pull` 可直接用；只有 443 被中断时才需要 API 绕行。
> 无论走哪条路，推送后养成跑一次 `sync-local.sh` 的习惯。

### 5. 业务数据看板（图表口径）

`pages/board.html` + `js/board.js`，图表用**仓库内**的 `vendor/echarts.min.js`（5.5.1），不外链 CDN —— 内部团队在国内网络下也能稳定加载。

| 图 | 内容 | 数据来源 |
|---|---|---|
| 日 GMV 趋势 | 国内 / 跨境双折线 | domestic + crossborder，按「日期」聚合 |
| 分店铺 GMV Top 10 | 横向条（**X 轴对数刻度**） | 同上，同店多日累计 |
| 平台 GMV 占比 | 环形 | 按店铺所属「平台」归类 |
| 负责人 GMV / 净利润 | 横向双系列条 | perf 表 |

**两条口径不可混用**（页面上也有标注）：

- **核心指标 = 国内 + 跨境 分店铺日报聚合**。日报总览的 GMV/订单/UV 是钉钉**公式字段**，OpenAPI 取不到，所以只能从分店铺聚合。
- **负责人业绩 = perf 表**，属「负责人认领口径」，与分店铺口径**不可直接相加**。

> 看板顶部有一条数据完整性提示：显示抓取了多少表/行、其中**有效记录**多少条、覆盖多少日期。
> 报表里绝大多数行是**预建的空日期占位行**（投放/内容/流量/供应链 4 表数值字段目前填充率为 0），
> 所以这 4 张表暂不出图 —— 等数据录入后加图即可。

## 📤 部署（GitHub Pages）

1. 在 GitHub 新建仓库 `team-portal`（Public）
2. 推送本目录内容到仓库
3. 仓库 **Settings → Pages → Source 选 `main` 分支 `/ (root)`** 保存
4. 等待约 1 分钟，访问 `https://alextok200-boop.github.io/team-portal/`

> 已含 `.nojekyll` 空文件，确保 GitHub Pages 不经过 Jekyll 处理（避免 `_` 开头目录被忽略）。

## 📝 变更日志

> 站点版本与资源版本分开：站点版本走语义化（v1.x.x），HTML 里的 `?v=` 是**缓存击穿号**（当前 js `2.4.0` / css `2.3.0`）。

- **v1.4.0**（2026-09-10）· 资源版本 `?v=2.4.0`：
  - 🐞 **修「新增用户登录总显示账号密码错误」**。根因不是密码错了，是**账号压根没到同事那台机器**：静态版把用户存在 localStorage，后台「新增用户」只写进**建号那台浏览器**；同事在别的设备登录时 `portal_users` 为空 → 回退到源码里的 3 个内置账号 → 查不到新账号 → 401。已复现钉死：同一浏览器建号→登录 **PASS**，全新浏览器登录 **FAIL**，而内置 `admin` 在全新浏览器 **PASS**（因为它在源码里）。
  - 新增 **`data/users.json` 共享账号名单**：用户表改为三源合并（源码内置 ⊂ 仓库名单 ⊂ 本机覆盖层，按 `username` 去重）。随仓库部署，**所有设备/浏览器通用**。
  - 管理后台新增 **「导出用户配置」**：一键下载 `users.json`，覆盖仓库 `data/users.json` 提交后全站生效。导出只含与源码/仓库**不等价**的账号，不重复导出内置账号。
  - 用户列表新增 **「来源」列**：`源码` / `仓库` / `待发布`（仅本机）——看到「待发布」就说明还没提交。新建/改密/删除后的提示语也都补上了「仅本机生效」。
  - 新增 **`tools/add-user.py`**：命令行加/改/停/删账号（算出 SHA-256 直接写 `data/users.json`），不打开浏览器也能加号。
  - 🔒 **移除不安全的降级哈希**：`crypto.subtle` 只在安全上下文（https / localhost / file）可用；历史版本在内网 IP（`http://192.168.x.x`）下会静默降级成 8 位 djb2 散列，导致**内网 IP 下建的号换成 https 后永远登不上** —— 与本次 bug 同一类失败。现已改为明确报错（「当前访问方式不安全…」），从源头杜绝「同一个号两种哈希」。新哈希带 `sha256$` 前缀并兼容历史裸 SHA-256。
  - 🐞 **修登录回写整表导致的陈旧快照**：原 `/api/login` 每次成功都 `saveUsers(整表)`，使成员浏览器留下当时的全量快照，管理员后来在仓库改密会被这份旧快照盖回去。现改为 `lastLogin` 存独立映射 `portal_lastlogin`，本机覆盖层只落差异记录、等价副本自动清理；删除源码内置账号改用 `deleted` **墓碑**，可随导出同步到全站。
  - 🐞 **修共享名单加载竞态**（本轮新写代码中自查发现）：管理后台的名单状态告警在 `data/users.json` 请求落地前就判断，读到 `{error:null}` 导致**拉取失败却不告警**。现改为 `API.remoteReady()` 等请求结束再判断，并在告警里带「重试」按钮。
  - 验证：新增 `tools/regress-user-flow.js` **26 项断言全过**（本机建号→登录 / 全新浏览器登录仓库账号 / 错误密码仍被拒 / 导出结构与 `sha256$` 前缀 / 删除内置账号墓碑与还原 / 非安全上下文明确报错 / 名单 404 告警可见）；全站 15 页冒烟 + 登录回归 4 项 + 15 项渲染断言，全部通过、0 报错。
- **v1.3.0**（2026-09-10）· 资源版本 `?v=2.3.0`：
  - **登录页改版：「加入我们」成为登录页的主要展示内容**。原先 `login.html` 只是一个居中登录框、背后空荡荡；现在整页展示招聘内容（Hero / 为什么是我们 / 4 个在招岗位 / 怎么投），登录表单退居**右侧吸顶栏**（≤1080px 时落到 hero 下方，保证首屏可见）。登录/权限逻辑与表单元素 ID 未变，功能零回归。
  - 新增 `js/landing.js`：岗位数据与渲染的**唯一副本**。此前 JOBS 在 `index.html`、`login.html`、`pages/careers.html` **各存一份**（三处副本），改一个岗位要改三个地方；现统一到该模块，三页共用。改岗位只需动 `JOBS` 数组。
  - ⚠️ **修掉 `<base>` 标签的片段锚点坑**：本页有 `<base href="/team-portal/">`，纯片段链接 `href="#jobs"` 会被解析成 `/team-portal/#jobs` —— 那是目录根（index.html），点「查看在招岗位」会**直接跳走**而不是本页滚动。已改为全路径（`login.html#jobs`、`login.html#loginRail`）。**新增锚点时务必带上文件名。**
  - 顶栏 `.landing-header` 加模糊底衬：登录页/首页都是长滚动页，原半透明渐变滚动时会与卡片文字叠在一起。
  - 全站 `css/portal.css?v=` 统一到 `2.3.0`（含 13 个 pages/*）。
  - 验证：桌面/移动两档 15 项渲染断言 + 登录功能回归 4 项（空密码拦截 / 错误密码拒绝并清空 / 正确密码进看板 / 已登录自动跳转）+ 全站 15 页冒烟，全部通过、0 报错。
- **v1.2.2**（2026-09-10）· 资源版本 `?v=2.2.0`：
  - **默认扫描上限 4000 → 20000**。依据：实测 9 张表里最长的两张（投放 / 内容）各 **7938 行**，上限低于表长会让 `truncated` 恒为真、误报「数据可能不完整」。
  - 修 `row_cap=0` 边界：原先传 0 会被当成有效值 → 只扫 1 页；现在非正整数一律回落到默认值（`posInt`）。
  - 新增 `emptyTables` 元数据，把「**整表扫完但零有效数据**」与「截断」「失败」三种状态区分开；日志逐表标注 `· 整表扫完，暂无数据`。**看板改为动态读取该字段，不再在前端硬编码表名。**
  - workflow `timeout-minutes` 15 → 20（全量扫描约 2.4 万行 ≈ 240 次分页，实测 4~5 分钟）。
  - **实测排除了一个假警报**：`投放 / 内容 / 流量 / 供应链 / 团队 / 日报总览` 6 张表扫到表尾仍零有效行，确认是**模板行尚未录入**，而非抓取截断或判空误杀（见下方「六张表的真相」）。
- **v1.2.1**（2026-09-10）· 资源版本 `?v=2.2.0`：
  - **抓取上限改双层**（`rawCap` 扫描 / `keepCap` 保留）+ 默认丢弃空白模板行。修掉 v1.2.0 暴露的机制缺陷：**上限原本按总行数计，空白模板行挤占配额、把真数据截断**。
  - 实测效果（对比 v1.1.x 上限 200 的产物）：分店铺有效记录 → **1,657 条**（国内 799 / 跨境 858）；真实交易日 **50 天**（07/23 – 09/10），此前混入整年空白模板行、虚报 151 天；**有 GMV 的店铺 19 → 36 家**；全公司 GMV ¥355,618.18 → **¥13,393,299.28**（此前低估 **37.7 倍**）。
  - 分店铺表扫描上限提至 16000 / 24000，`data/daily.json` 由 3.2MB 回落到 ~1MB 量级。
  - 看板顶部提示条改为「保留 N 条有效记录（扫描 M 行，已过滤 K 行空白模板行）」，并在有表触顶时显式告警。
- **v1.2.0**（2026-09-10）· 资源版本 `?v=2.2.0`：
  - **看板增强**：新增 4 张图表（日 GMV 趋势 / 分店铺 Top10 / 平台占比 / 负责人业绩），逻辑抽到 `js/board.js`；ECharts 5.5.1 **入库**到 `vendor/`（不外链 CDN）；指标卡加副标（占比 / 费比）；新增数据完整性提示条与负责人业绩小结。
  - **抓取配置调整**：每表行数上限 200 → 4000；新增 `truncated` / `filledRows` / `failedTables` 元数据；HTTP 层加**指数退避重试**；抓取改为**每日两次**（11:05 / 18:30）；workflow 推送前加 `git pull --rebase` + `concurrency` 锁，避免与人工推送撞车。
  - 统一全站 `css/portal.css?v=2.2.0`（原先 2.0.0/2.1.0 混用）。
- **v1.1.1**（2026-09-10）：同步脚本加固
  - 双通路：`git fetch` 不通时自动走 `api.github.com` 兜底（新增 `tools/fetch-remote-commit.py`，在本地重建同 SHA 的 commit 对象）
  - 新增**防误删闸门** `--force`：工作区脏时先备份再停下，不再无声丢弃未提交改动
  - 修掉两个隐蔽坑：`git update-ref` 在本环境静默失效（改直写兜底）；Windows 下 Python 文本模式把 `\n` 转成 `\r\n` 致重建 SHA 永不匹配（改字节流）
  - 新增 `.gitignore`（`__pycache__`、API 推送临时产物）
- **v1.1.0**（2026-09-10）：
  - 新增 `tools/sync-local.sh` —— 本地仓库对齐远端 main；修复本地 git 与远端历史分叉（本地曾只有 1 个 commit，远端已 8 个）。
  - README 补齐：业务数据看板 / 数据中心 / 钉钉自动抓取链路 / 角色权限表 / 完整目录结构。
  - **修正过时说明**：原「日报数据更新方式」仍写手工从 login-portal 拷 JSON，实际早已改为 GitHub Actions 自动抓取。
- **静态版 v1.0.0**（2026-09-10）：由 login-portal v2.0.0 改造为纯前端静态版，
  适配 GitHub Pages 子路径部署，登录/权限/数据全部本地化。



